/**
 * Supported languages for Cobbler app (English and Kannada).
 */
const COBBLER_SUPPORTED_LANGUAGES = ['en', 'kn'];
const DEFAULT_LANGUAGE = 'en';

module.exports = {
  COBBLER_SUPPORTED_LANGUAGES,
  DEFAULT_LANGUAGE,
};
